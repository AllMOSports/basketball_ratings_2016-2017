name: 2016-2017 Basketball Ratings

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-ratings:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests beautifulsoup4 numpy

      - name: Run ratings script
        run: python basketball_ratings_2016-2017.py

      - name: Commit and push basketball_ratings_2016-2017.json
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add .
          git diff --staged --quiet || git commit -m "Update ratings $(date +'%Y-%m-%d')"
          git push
